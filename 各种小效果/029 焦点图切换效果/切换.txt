图片切换
1 所有图片position:absolute定位在一个地方
	1 修改z-index：
  		1 设定一个全局变量zIndex，一直++
  		2 每次所有z-index:0,切换到的z-index:1
	2 修改display none/inline
	3 修改opacity

2 图片在不同位置，修改left或top值
  注："2 用fnMove替代transition.html"放到IE8-里用不了，因为section新标签，ECMA6中的let等还没做兼容。

3 没有按钮的